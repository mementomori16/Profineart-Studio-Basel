# .github/workflows/deploy-on-merge.yml (or similar file name)

# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          # UPDATED: New service account secret name
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_PROFINEART_STUDIO_BASEL }}
          channelId: live
          # UPDATED: New project ID
          projectId: profineart-studio-basel 
        env:
          FIREBASE_CLI_EXPERIMENTS: webframeworks
